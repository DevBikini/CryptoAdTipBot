/*CMD
  command: /test
  help: 
  need_reply: false
  auto_retry_time: 
  folder: Test command 
  answer: 
  keyboard: 
  aliases: 
CMD*/

/*var json = getOnline(3)
for (var ind in json) {
  Bot.inspect(json[ind])
}

var grab = Bot.getProperty("grab=" + request.chat.id, { list: {} })
Bot.inspect(grab)
Bot.setProperty("grab=" + request.chat.id, "", "string")*/
